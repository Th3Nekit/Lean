<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Do not back up the local profile store or settings by default;
         the user can export/import explicitly. -->
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
</full-backup-content>
